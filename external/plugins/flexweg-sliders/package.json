{
  "name": "flexweg-cms-sliders-plugin",
  "private": true,
  "version": "1.0.0",
  "description": "Adds four slider blocks (image, hero, cards, logo carousel) to the Flexweg post/page editor. Per-page detection injects the runtime JS + CSS only on pages that use a slider.",
  "type": "module",
  "scripts": {
    "build": "vite build && node scripts/pack.mjs",
    "pack": "node scripts/pack.mjs"
  },
  "devDependencies": {
    "@tiptap/core": "^2.10.3",
    "@tiptap/react": "^2.10.3",
    "@types/react": "^18.3.28",
    "@vitejs/plugin-react": "^4.3.3",
    "jszip": "^3.10.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-i18next": "^15.2.0",
    "typescript": "^6.0.3",
    "vite": "^5.4.10"
  }
}
